//30-sum of Nth and Mth  prime number 

import java.util.Scanner;
class SumOfNthAndMth
{
	public static void main (String [] args){
	Scanner scan = new Scanner (System.in);
	System.out.println("Enter M NUmber ");
	int M = scan.nextInt();
	System.out.println("Enter N number ");
	int n = scan.nextInt();
	int sln =1;
	int sum=0;
	for (int a=1;sln<=n ;a++ )
	{
		int count=0;
		for (int i=1;i<=a ;i++ )
		{
			if (a%i==0)
			{
				count++;
			}
		}
		if (count == 2)
		{
			System.out.printin("print " +x);
			if (sln ==M || sln ==n)
			{
				
				sum=sum+a;
			}
			sln++;
		}
		
		}
		System.out.println("sum of "+n+"th prime and "+M+" th prime is "+sum);
	}
}